cache/
config.local.json
__pycache__/
*.pyc
*.out.log
*.err.log
data/launches_*.jsonl
data/listener_state.json
data/listener_events.jsonl
data/ledger_s.csv
data/paper_ledger_s.csv
data/paper_positions_s.json

# live multi-policy paper book (selfimprove/livebook.py) — local state, rewritten every 60s.
# Same rationale as the S book above: it must never conflict with what the cloud commits back,
# and a file that changes every minute would be pure churn in git history.
data/livebook.json
data/livebook_fills.csv
data/livebook_ticks.jsonl
data/livebook_feed.json
data/livebook_missed.jsonl
